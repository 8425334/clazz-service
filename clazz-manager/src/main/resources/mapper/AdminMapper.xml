<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.deb.clazz.manager.mapper.AdminMapper">
    <insert id="create">
        insert into
            t_admin(username, password, phone, name, super)
        values
            (#{p.username}, #{p.password}, #{p.phone}, #{p.name}, #{p.super})
    </insert>
</mapper>